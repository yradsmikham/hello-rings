apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.application.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.application.name }}
    ring: {{ .Values.ring.name }}
    claim: {{ .Values.ring.claim }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{ toYaml .Values.application.selector }}
  template:
    metadata:
      labels:
        {{ toYaml .Values.application.selector }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            {{ toYaml .Values.application.ports }}
