trigger:
  branches:
    include:
    - '*'
  paths:
    include:
    - 'remove-ring.yml'

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'hello-rings-acr'
    repository: 'gc_hellorings_test'
    command: 'buildAndPush'
    Dockerfile: '**/src/Dockerfile'
    tags: 'gc-hello-rings-$(Build.SourceBranchName)-$(Build.BuildId)'
